# Copy this file to .env and adjust values
# Server
PORT=4000
# Allow your Next.js site origin during dev (comma-separated if multiple)
CORS_ORIGIN=http://localhost:3000

# PostgreSQL connection (pgAdmin can manage this DB)
# Example for local Postgres:
# DATABASE_URL=postgres://username:password@localhost:5432/luxurystay
DATABASE_URL=
# Set to 'require' if using managed DB that enforces SSL
PGSSL=